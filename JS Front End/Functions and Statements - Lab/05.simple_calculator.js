function solve(a, b, op) {
    switch (op) {
        case "multiply":
            console.log(a * b); break;
        case "divide":
            console.log(a / b); break;
        case "add":
            console.log(a + b); break;
        case "subtract":
            console.log(a - b); break;
    }
}